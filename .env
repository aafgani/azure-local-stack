# Path to the bus-config.json you created in Step 1
CONFIG_PATH="./config/asb/config.json"

# Set to 'Y' to accept the Microsoft EULA for both SQL and the Emulator
ACCEPT_EULA="Y"

# Create a strong password for the SQL back-end
MSSQL_SA_PASSWORD="YourSecurePassword123!"